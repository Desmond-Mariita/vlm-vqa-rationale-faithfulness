# Data access

Acquire VCR through https://visualcommonsense.com/ after reviewing https://visualcommonsense.com/license/ . This release supplies no VCR JSON records, image corpus, general-purpose questions/choices/rationales, frozen descriptions or raw generation shards. Follow the dataset distributor's current acquisition process; no credentials or signed download links are included.

The expected local inputs use `data/raw/`, `data/processed/vcr/{train,val}/images/`, `data/final/{train,val}/images/`, and `data/final/vcr_train_5_pct.json` / `vcr_val_10_pct.json`. Documentation-only stubs under `data/external/` inventory external artifacts and known hashes. Keep locally acquired data ignored; never add it to a release allow-list.

The fixed subsets contain 10,646 training and 2,653 validation records (10,056 and 2,400 source frames); the recorded frame overlap is zero. Frozen descriptions and rationale-bearing execution manifests need separately authorised access. Access is not promised. Generated image interventions and any new captions remain external. VCR imagery, including the 43 former thesis raster dependencies, is not redistributed in the current repository tree. This repository grants no rights to VCR data.

The unchanged thesis source deliberately references absent visual assets under `thesis/Figures/records/` and `thesis/Figures/w2c/panels/`. Scientific captions, example text and TeX figure definitions remain intact. The current image policy permits zero VCR rasters. The `historical_vcr_files` section of `provenance/image_policy.json` retains the exact 43 paths, hashes and source references as metadata only; `provenance/thesis_dependencies.json` marks those dependencies withheld. Historical classifications do not permit payload inclusion.

Numeric reproduction (`make check`, including all 12 table comparisons) and code inspection need no redistributed VCR imagery. `python scripts/validate_release.py --image-only` checks the current payload; `make thesis-status` verifies source integrity and reports withheld dependencies. Neither command claims full PDF reproducibility. `make -C thesis pdf` cannot complete from this source package alone: its image-inclusive build needs separately licensed local VCR assets and the independently withheld University logo. No replacement image or canonical placeholder is supplied.

For a licensed local reconstruction:

1. Obtain VCR from the [official distributor](https://visualcommonsense.com/) after accepting its own [dataset licence](https://visualcommonsense.com/license/) and applicable terms. Follow `data/external/` stubs and `data_prep/canonical/README.md` for the recorded input layout; no access or exact external archive is promised by this repository.
2. In a separate local build copy outside the Git release checkout, supply the licensed raw/processed frames and separately authorized frozen material required by the original figure builders. `scripts/build_manuscript_record_figures.py` records the M/C/R selection and image interventions; `scripts/build_w2c_record_panels.py` records the 16 appendix panel pairs. Inspect their input constants and hash guards before use. Both historical builders also write TeX/audit files, so do not run them over the canonical release source or substitute newly generated captions/results.
3. Use the preserved historical path/hash list to verify the required local rasters and place them at the exact source-relative paths in that separate build copy. Exact reconstruction additionally depends on the original external subsets, manifests and frozen outputs; fresh data or generations are not asserted to be equivalent. Keep all derived imagery and image-inclusive PDFs outside Git. Preserve the canonical TeX source and all scientific values.

This omission follows the distributor's restrictions on redistribution; the repository does not provide VCR licence text, raw records, image payloads, or a grant of data rights. This repository was created from a single clean root commit and contains no VCR raster payload in its current tree or reachable Git history. The separate original audit/history repository remains private. Public visibility does not grant VCR rights.
