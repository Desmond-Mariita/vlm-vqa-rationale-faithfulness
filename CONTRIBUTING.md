# Contributing

Desmond Mariita is the sole scientific decision-maker, publication authority and merge/push authority. Propose changes through review; do not update frozen values, prompts, parser rules, or experiment definitions incidentally. Run `make check` on CPU. Changes to source-derived files require a new provenance record and renewed validation. Never submit dataset payload, raw generation text, model weights, private review files, or unapproved imagery. Licensing remains pending.
